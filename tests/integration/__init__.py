"""Integration tests against the live Kohler Anthem API.

These tests require valid credentials and make real network calls. They are
skipped automatically when credentials are not available.

Run with:
    pytest tests/integration/

Or via Make:
    make health-check
"""
